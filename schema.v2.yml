# Define the chain to run on
chain: ethereum

# The contracts to populate tables for
contracts:
  - address: 0xCEAF7747579696A2F0bb206a14210e3c9e6fB269
    name: ust_curve
    abi: curve_pool.abi.json
    #if event 'event' if read contract 'methods'
    #where do i put inputs
    methods:
      - name: get_virtual_price
        outputs:
          - price
