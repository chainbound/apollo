# Define the chain to run on
chain: arbitrum

# The contracts to populate tables for
contracts:
  - address: 0x905dfCD5649217c42684f23958568e533C711Aa3
    name: usdc_eth_events
    abi: unipair.abi.json
    methods:
      - name: getReserves
        outputs:
          - _reserve0
          - _reserve1
